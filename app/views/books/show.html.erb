<%= render 'users/user_info',user: @user %>
<%= render 'new',book: @book_new %>

<h1>Book detail</h1>
<table>
  <tr>
    <th></th>
    <th>Title</th>
    <th>Opinion</th>
  </tr>
  <tr>
    <td></td>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
    <% if @book.user_id == current_user.id %>
    <td><%= link_to "Edit",edit_book_path(@book.id) %></td>
    <td><%= link_to "Destroy",book_path(@book.id),method: :delete,"data-confirm"=>"後悔しませんね？" %></td>
    <% end %>
  </tr>
</table>