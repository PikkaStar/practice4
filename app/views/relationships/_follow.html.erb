<% if current_user.following?(user) %>
  <%= link_to "フォロー外す",user_relationships_path(user.id),remote: true,method: :delete,class: "btn btn-danger form-control" %>
  <% else %>
  <%= link_to "フォローする",user_relationships_path(user.id),remote: true,method: :post,class: "btn btn-success form-control" %>
   <% end %>