<h1><%= @user.name %>のいいね一覧</h1>

<table class="table">
        <tbody>
          <tr>
            <th>Image</th>
            <th>Name</th>
            <th>Title</th>
          </tr>

          <% @favorite.each do |favorite| %>
          <tr>
            <td><%= image_tag favorite.user.get_profile_image(100,100) %></td>
            <td>
               <%= link_to user_path(favorite.user.id) do %>
              <%= favorite.user.name %>
              <% end %>
              </td>
            <td>
              <%= favorite.title %>
            </td>

          </tr>
          <% end %>
        </tbody>
      </table>